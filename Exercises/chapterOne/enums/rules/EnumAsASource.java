package enums.rules;

public enum EnumAsASource {

	/**
	 * These are constants, as per naming convention, it should be caps
	 */
	SINGLE, DOUBLE,
	/**
	 * But are not restricted to caps
	 */
	tripple, $one, _one, with_number_123,
}
